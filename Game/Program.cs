

namespace Example
{
	internal class Program
	{
		private static void Main()
		{
			// var window = ExampleWindow.Create();
			//var model = new Model(); // model does not know about the view
			//var view = new View();
			//window.UpdateFrame += args => model.Update((float)args.Time, window.KeyboardState); // call update once each frame
			//window.Resize += args => view.Resize(args.Width, args.Height); // on window resize let the view do whats needs to be done
			//window.RenderFrame += _ => view.Draw(model); // first draw the model
			//window.RenderFrame += _ => window.SwapBuffers(); // then wait for next frame and buffer swap
			//window.Run(); // start the game loop with 60Hz
		}
	}
}